#include "main.h"

/**
 * _memset - fills a memory block with a constant byte
 * @s: address to mem block
 * @b: char to be used
 * @n: number of bytes
 *
 * Return: pointer to the memory block
 */

char *_memcpy(char *dest, char *src, unsigned int n)
{
	while (n)
	{
		s[n-1] = b;
		n--;
	}
	return (s);
}
