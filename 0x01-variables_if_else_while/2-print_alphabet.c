#include <stdio.h>
/**
 * main - print lowercase alphabets
 *
 * Return: always 0
 */
int main(void)
{
	char c;

	for (ch = 'a' ; ch <= 'z' ; ch++)
	{
		putchar(ch);
	}
	putchar('\n');
	return (0);
}
